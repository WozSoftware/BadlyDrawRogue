namespace Woz.RogueEngine.Entities
{
    public enum EntityFlags
    {
        IsHostile,
        HasSeenPlayer,

        IsShield,
        IsWeapon,
        IsClothing,
        IsEquiped,
        IsFood,
        IsDrink,
        IsPotion,

        IsOpen,

        BlocksMovement,
        BlocksLineOfSight,
    }
}